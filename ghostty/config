## these don't work for some reason
# command = zsh
# initial-command = /opt/homebrew/bin/tmux new-session -A -s config -c "$XDG_CONFIG_HOME"

# macos option should be treated as Alt
macos-option-as-alt=true

# aesthetics
theme = TokyoNight Storm
font-family = JetBrains Mono Nerd Font Mono
background-opacity = 0.85
background-blur=16
background=#000000

# typography
font-size=16
font-thicken=true
font-thicken-strength=1
adjust-cell-height=1

# map CMD+S to Ctrl+S (for saving files in nvim)
keybind = super+s=text:\x13

# TMUX settings (\x1b\x20 = ALT+Space)

# Pane navigation
# left (sends ctrl+h)
keybind = super+h=text:\x08
# down (sends ctrl+j)
keybind = super+j=text:\x0a
# up (sends ctrl+k)
keybind = super+k=text:\x0b
# right (sends ctrl+l)
keybind = super+l=text:\x0c

# close
keybind = unconsumed:super+w=text:\x1b\x20x
# zoom
keybind = unconsumed:super+z=text:\x1b\x20z

# select window
keybind = unconsumed:super+shift+w=text:\x1b\x20w
# prev session
keybind = unconsumed:super+shift+h=text:\x1b\x20(
# next session
keybind = unconsumed:super+shift+l=text:\x1b\x20)

# open lazygit in popup
keybind = unconsumed:super+g=text:\x1b\x20g

# split window (vertical)
keybind = unconsumed:super+shift+backslash=text:\x1b\x20|
# split window (horizontal)
keybind = unconsumed:super+shift+minus=text:\x1b\x20_

# switch to window
keybind = unconsumed:super+shift+digit_1=text:\x1b\x201
keybind = unconsumed:super+shift+digit_2=text:\x1b\x202
keybind = unconsumed:super+shift+digit_3=text:\x1b\x203
keybind = unconsumed:super+shift+digit_4=text:\x1b\x204
keybind = unconsumed:super+shift+digit_5=text:\x1b\x205
keybind = unconsumed:super+shift+digit_6=text:\x1b\x206
keybind = unconsumed:super+shift+digit_7=text:\x1b\x207
keybind = unconsumed:super+shift+digit_8=text:\x1b\x208
keybind = unconsumed:super+shift+digit_9=text:\x1b\x209
keybind = unconsumed:super+shift+digit_0=text:\x1b\x200

# switch tmux window prev / next
keybind = unconsumed:super+shift+p=text:\x1b\x20p
keybind = unconsumed:super+shift+n=text:\x1b\x20n

# CMD+SHIFT+s to open session switcher
keybind = unconsumed:super+shift+s=text:\x1b\x20s
# CMD+SHIFT+o to open session switcher
keybind = unconsumed:super+shift+o=text:\x1b\x20o

# CMD+t to open new tmux window (uses custom bind t to preserve current path)
keybind = unconsumed:super+t=text:\x1b\x20t

# vim: set ft=conf :
